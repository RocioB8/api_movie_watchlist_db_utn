meta {
  name: Update Movie
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/movies/{{movieId}}
  body: json
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "year": 2010
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
