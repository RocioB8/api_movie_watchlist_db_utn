meta {
  name: Delete Movie
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/movies/{{movieId}}
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}

